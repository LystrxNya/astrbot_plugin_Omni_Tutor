{
    "dashscope_key": {
        "title": "阿里云百炼 (DashScope) API Key",
        "description": "用于调用 Qwen 和 DeepSeek 模型",
        "type": "string",
        "default": ""
    },
    "siliconflow_key": {
        "title": "硅基流动 API Key",
        "description": "用于调用 Embedding 向量和重排模型",
        "type": "string",
        "default": ""
    },
    "fast_model": {
        "title": "极速打标模型 (Fast Brain)",
        "description": "必须支持 JSON 结构化输出。",
        "type": "string",
        "default": "qwen-flash"
    },
    "reasoning_model": {
        "title": "深度推理模型 (Reasoning Brain)",
        "description": "用于多意图裂变和精读。必须支持 JSON 结构化输出。",
        "type": "string",
        "default": "qwen3.5-plus"
    },
    "ocr_model": {
        "title": "OCR 基础模型",
        "description": "用于基础文字识别。默认: qwen-vl-ocr",
        "type": "string",
        "default": "qwen-vl-ocr"
    },
    "vision_model": {
        "title": "视觉大模型 (Max)",
        "description": "用于复杂几何图形理解。默认: qwen-vl-max",
        "type": "string",
        "default": "qwen-vl-max"
    },
    "embedding_model": {
        "title": "向量化模型",
        "description": "硅基流动平台的 Embedding 模型",
        "type": "string",
        "default": "BAAI/bge-m3"
    },
    "rerank_model": {
        "title": "重排模型",
        "description": "硅基流动平台的 Rerank 模型",
        "type": "string",
        "default": "BAAI/bge-reranker-v2-m3"
    }
}